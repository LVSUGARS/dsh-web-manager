<Window x:Class="DshLauncher.App.SplashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DSH Web Launcher" Width="1080" Height="700" MinWidth="720" MinHeight="480"
        FontFamily="Microsoft YaHei UI"
        WindowStyle="None" ResizeMode="CanResize" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" ShowInTaskbar="True" PreviewMouseLeftButtonDown="Window_PreviewMouseLeftButtonDown">
    <Border x:Name="Shell" CornerRadius="9" BorderBrush="#4A6A8D" BorderThickness="1" Background="#050A13" ClipToBounds="True" SnapsToDevicePixels="True" UseLayoutRounding="True">
        <Grid>
            <Rectangle Fill="{DynamicResource GridBrush}" IsHitTestVisible="False" />
            <Rectangle>
                <Rectangle.Fill>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                        <GradientStop Color="#0A1830" Offset="0" />
                        <GradientStop Color="#060F1C" Offset="0.55" />
                        <GradientStop Color="#030810" Offset="1" />
                    </LinearGradientBrush>
                </Rectangle.Fill>
                <Rectangle.OpacityMask>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="0,1"><GradientStop Color="#00000000" Offset="0.10"/><GradientStop Color="#B3000000" Offset="0.96"/></LinearGradientBrush>
                </Rectangle.OpacityMask>
            </Rectangle>
            <Ellipse x:Name="LogoGlow" Width="200" Height="200" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.34" IsHitTestVisible="False">
                <Ellipse.Fill>
                    <RadialGradientBrush>
                        <GradientStop Color="#5C8FD6" Offset="0" />
                        <GradientStop Color="#122A4A" Offset="0.55" />
                        <GradientStop Color="#00000000" Offset="1" />
                    </RadialGradientBrush>
                </Ellipse.Fill>
            </Ellipse>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,0,0,16">
                <Viewbox x:Name="WhaleImage" Width="82" Height="82" HorizontalAlignment="Center">
                    <Path Data="{StaticResource WhaleGeometry}" Fill="#F5F8FF" Stretch="Uniform" />
                </Viewbox>
                <TextBlock Text="DSH Web Launcher" Foreground="#F5F8FF" FontSize="38" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,18,0,0" />
                <TextBlock Text="DEEPSEEK HARNESS  ·  LOCAL CONSOLE" Foreground="#8FA6C2" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,9,0,0" />
                <Border Height="1" Width="72" Background="#78A8FF" Opacity="0.72" HorizontalAlignment="Center" Margin="0,22,0,20" />
                <TextBlock x:Name="EnterHint" Text="探索未至之境" Foreground="#E7F0FF" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" />
                <TextBlock x:Name="VersionText" Text="v2.0.0  ·  LVSUGARS" Foreground="#7F94AF" FontSize="10" HorizontalAlignment="Center" Margin="0,12,0,0" />
            </StackPanel>
            <StackPanel x:Name="WindowControls" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,16,18,0">
                <Button Content="-" Width="36" Height="30" Padding="0" Foreground="#E7F0FF" FontSize="16" FontWeight="SemiBold" ToolTip="最小化" Click="Minimize_Click" />
                <Button Content="□" Width="36" Height="30" Padding="0" Foreground="#E7F0FF" FontSize="16" FontWeight="SemiBold" ToolTip="最大化" Click="Maximize_Click" />
                <Button Content="×" Width="36" Height="30" Padding="0" Foreground="#E7F0FF" FontSize="16" FontWeight="SemiBold" ToolTip="关闭" Click="Close_Click" />
            </StackPanel>
            <TextBlock Text="ESC 退出" Foreground="#8497AF" FontSize="10" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,24,18" Opacity="0.76" />
        </Grid>
    </Border>
</Window>
